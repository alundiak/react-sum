# https://www.appveyor.com/docs/lang/nodejs-iojs/

version: 1.0.{build}

branches:
  only:
  - main

skip_tags: true

image: Ubuntu

environment:
  matrix:
    - nodejs_version: "22"
    - nodejs_version: "23"
    - nodejs_version: "24"

install:
- sh: npm ci

build_script:
- sh: npm run build

test_script:
- sh: npm test
